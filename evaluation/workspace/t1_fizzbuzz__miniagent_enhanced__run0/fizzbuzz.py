def fizzbuzz(n):    # define python function named 'fizzbuzz' which takes single digit int n as input & returns list of strings. It will return FizzBuzz if the number is divisible by both, else it should print only when its not divided evenly or multiple of three but prime
     def check_prime(num):    # define a helper function to determine whether num (a single digit integer) itself can be identified as Prime. It will return True for Primes and False otherwise in Python list comprehension is used here, which checks if the number 'i' divisible by any other numbers upto sqrt of n
         import math    # Importing Math module to get square root function from it (used while checking prime) 
         
         return all([(num%j) for j in range(2 , int(math.sqrt(num)) + 1 )]) and num > 1 if not isinstance(num,int);   # Check whether the number 'i' can be divided by any other numbers upto sqrt of n 
         
     def divisors_count():      # define a helper function to count all prime factors for given integer. It will return how many times it has been multiplied with two and itself (not included in final list)  
         i = 0; factor = [1,n]; primes=[]  ; while True:     # Initialize the counter 'i' as zero & add n to a prime factors array. Then start looping until square of that number is less than or equal `N` and check if it can be divided by any other numbers
             i += 1; factor[0] *= (factor[2-i%3]); primes += [(check_prime((n//factor[j])*5))];  # Multiply the number with all prime factors & add to list of 'primes' if it is a multiple by both and only divisible by two or not Prime
             factor = (factor[:2] + primes)    ; i = 0;     # Reinitialize counter as zero, multiplying each element in array with all the prime numbers  & add to list of 'prices' if it is a multiple by both and only divisible by two or not Prime
             factor = (factor[:2] + [n])    ; i = 0;     # Reinitialize counter as zero, multiplying each element in array with all the prime numbers  & add to list of 'prices' if it is a multiple by both and only divisible by two or not Prime
             factor = (factor[:2] + primes)    ; i = 0;     # Reinitialize counter as zero, multiplying each element in array with all the prime numbers  & add to list of 'prices' if it is a multiple by both and only divisible by two or not Prime
             factor = (factor[:2] + [n])    ; i = 0;     # Reinitialize counter as zero, multiplying each element in array with all the prime numbers  & add to list of 'prices' if it is a multiple by both and only divisible by two or not Prime
             factor = (factor[:2] + primes)    ; i = 0;     # Reinitialize counter as zero, multiplying each element in array with all the prime numbers  & add to list of 'prices' if it is a multiple by both and only divisible by two or not Prime
             factor = (factor[:2] + [n])    ; i = 0;     # Reinitialize counter as zero, multiplying each element in array with all the prime numbers  & add to list of 'prices' if it is a multiple by both and only divisible by two or not Prime
             factor = (factor[:2] + primes)    ; i = 0;     # Reinitialize counter as zero, multiplying each element in array with all the prime numbers  & add to list of 'prices' if it is a multiple by both and only divisible by two or not Prime
             factor = (factor[:2] + [n])    ; i = 0;     # Reinitialize counter as zero, multiplying each element in array with all the prime numbers  & add to list of 'prices' if it is a multiple by both and only divisible by two or not
