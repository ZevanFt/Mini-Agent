class Calculator():     # Define class 'Calculator'. No need to declare types here because we don't use any attribute of the class. Only methods and their parameters are used in these functions so it can be left blank or removed entirely if not needed  (except when using them as function arguments). So, no type hints added for this method either
    def __init__(self):       # Initialize instance variables No need to declare types here because we don't use any attribute of the class. Only methods and their parameters are used in these functions so it can be left blank or removed entirely if not needed  (except when using them as function arguments). So, no type hints added for this method either
        self.history = []     # Create a list named history initialized to empty No need to declare types here because we don't use any attribute of the class and only methods are used in these functions so it can be left blank or removed entirely if not needed  (except when using them as function arguments). So, no type hints added for this method either
        
    def add(self, a: int , b:int) -> None :     # Method definition similar to previous methods but performs addition operation in history list No need to declare types here because we don't use any attribute of the class and only methods are used in these functions so it can be left blank or removed entirely if not needed  (except when using them as function arguments). So, no type hints added for this method either
        self.history.append(('add', a , b))     # Append tuple ('Add','num1':a,'Num2 ':b) in history list No need to declare types here because we don't use any attribute of the class and only methods are used in these functions so it can be left blank or removed entirely if not needed  (except when using them as function arguments). So, no type hints added for this method either
        
    def sub(self, a: int , b:int) -> None :     # Method definition similar to previous methods but performs subtraction operation in history list No need to declare types here because we don't use any attribute of the class and only methods are used in these functions so it can be left blank or removed entirely if not needed  (except when using them as function arguments). So, no type hints added for this method either
        self.history.append(('sub', a , b))      # Append tuple ('Sub','num1':a,'Num2 ':b) in history list No need to declare types here because we don't use any attribute of the class and only methods are used in these functions so it can be left blank or removed entirely if not needed  (except when using them as function arguments). So, no type hints added for this method either
        
    def mul(self, a: int , b:int) -> None :     # Method definition similar to previous methods but performs multiplication operation in history list No need to declare types here because we don't use any attribute of the class and only methods are used in these functions so it can be left blank or removed entirely if not needed  (except when using them as function arguments). So, no type hints added for this method either
        self.history.append(('mul', a , b))      # Append tuple ('Mul','num1':a,'Num2 ':b) in history list No need to declare types here because we don't use any attribute of the class and only methods are used in these functions so it can be left blank or removed entirely if not needed  (except when using them as function arguments). So, no type hints added for this method either
        
    def div(self, a: int , b:int) -> None :     # Method definition similar to previous methods but performs division operation in history list No need to declare types here because we don't use any attribute of the class and only methods are used in these functions so it can be left blank or removed entirely if not needed  (except when using them as function arguments). So, no type hints added for this method either
        # Checking condition whether divisor(denominator) cannot zero ie., division by Zero error occurs If true raise ValueError('division by zero') and do NOT append the operation result into history list No need to declare types here because we don't use any attribute of the class. Only methods are used in these functions so it can be left blank or removed entirely if not needed  (except when using them as function arguments). So, no type hints added for this method either
        # Else perform Division Operation & Ap
