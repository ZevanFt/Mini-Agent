def flatten(obj: dict, sep: str = '.') -> dict:     # Define the main recursive method. It takes in an object and returns nothing if it's not dictionary otherwise starts here with current iteration containing two values – one being key part of iterable (key-value pair)
    output_dict  = {}   # Initialize a new variable named 'output', which will store our final result, start out as empty because we haven’t added anything yet in it. When function gets called again on subdictionary this time – adding something into its place then return that thing back up through all of these calls
    for key_main , value   in obj .items() : # Iterate over each item (key-value pair) inside your dictionary, the variable 'item' is going to be current iteration which will contain a tuple with two values – one being main part and another that we are working on. If there’s no further nesting levels left
        if not key_main: #If it isn't an empty string then add into our final result as-is, this is because we don’t want lists (or other iterable types) in the output dict anymore and just take their values directly – this will be done by simply adding them to 'output'.
            if not value: # If it's an empty string then add into our final result as-is, This is because we don’t want lists (or other iterable types) in the output dict anymore and just take their values directly – this will be done by simply adding them to 'output'.
            else :   # Otherwise if it's not an empty string then add into our final result with a separator, This is because we don’t want lists (or other iterable types) in the output dict anymore and just take their values directly – this will be done by simply adding them to 'output'.
    return(output_dict.copy()) # Returning from function after all iterations are completed with no more changes made on it, so we need a copy of final result for further use or modification if required in future without affecting the original one which is stored inside output variable – this will be done by simply copying 'final' into its place
