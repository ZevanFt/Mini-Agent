# 2026-05-25 日志（下半段）

## 今日工作（续）

### 6. MiniAgent CLI 实测跑通 ✅

首次用 MiniAgent CLI + qwen2.5-coder:3b 跑真实编码任务。

**问题发现与修复：**

| 问题 | 修复 | 文件 |
|------|------|------|
| 小模型把 tool_call 写成 JSON 嵌入 ```json 块 | 增加 `extractToolCallsFromText()` 回退解析器 | `src/llm/ollama.ts` |
| Phase 8 增强器在 qwen 3B 上无必要地拖慢 5x | 实现 `autoDetectStrategy()` 模型自适应禁用 | `src/core/agent.ts` |
| CLI 没把模型名传给 Agent | 补充 `model: currentModel` 参数 | `src/cli.ts` |

**自适应策略逻辑（已实现）：**

| 模型类型 | Phase 8 增强 | 理由 |
|---|---|---|
| `qwen2.5-coder:3b` | 🔴 关闭 | 裸模型 100% 通过，增强是拖累 |
| `deepseek-coder:1.3b` | 🟢 开启 | 裸模型 6.7%，需要后处理兜底 |
| `qwen2.5-coder:7b` | 🔴 关闭 | 大模型不需要 |
| 其他 ≥7B | 🔴 关闭 | |

### 7. 项目整理

- `src/llm/ollama.ts` — 添加 `extractToolCallsFromText()` 函数
- `src/core/agent.ts` — 添加 `autoDetectStrategy()` + 模型自适应
- `src/cli.ts` — 传递 `model` 参数到 Agent
- `.gitignore` 更新：只忽略 `__pycache__`，保留 workspace 供存档

### 8. 遗留问题（已知不阻塞）

以下 TypeScript 类型错误是项目原有的，非本次改动引入：
- `FormatTool` 未定义（`src/cli.ts:214`）
- `logger` 未定义（`src/core/agent.ts:364`）
- `ThinkingMode` 类型不匹配（`src/cli.ts:543`）
- `enhancer/index.ts` 多处类型错误
- `commands.ts: reloadSkills` 方法不存在

## 最终结论

| 维度 | 结论 |
|------|------|
| qwen2.5-coder:3b 裸模型 | 🏆 **5/5 全过，100%** |
| ds-coder:1.3b + 后处理器 | 从 6.7% 提升到 42.5% |
| CLI tool_call 回退解析 | ✅ 适配小模型 JSON 格式 |
| 自适应策略 | ✅ 按模型名自动开关增强 |
| 评测框架 | ✅ 可复现，可扩展新模型 |
| 日记系统 | ✅ `journal/` + `INDEX.md` |
| 模型部署脚本 | ✅ `local_llm/scripts/manage.ps1` |

## 下一个版本计划

- [ ] 修复预存的 TypeScript 类型错误
- [ ] 后处理器 Python 代码集成到 TypeScript 管线
- [ ] `bun` 兼容性检查（项目用 bun 但机器没装）
