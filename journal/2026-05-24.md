# 2026-05-24 日志

## 今日工作

### 1. 环境搭建

- 在 G 盘安装了 Ollama 0.24.0（解压版 `G:\\ollama\\app`）
- 配置 `OLLAMA_MODELS=G:\\ollama\\models`，模型存储到 G 盘，避开 C 盘空间不足
- 拉取 `deepseek-coder:1.3b`（776MB, Q4 量化）
- 验证推理速度：~47 tok/s（CPU）

### 2. 评测框架搭建

创建了 `evaluation/` 目录结构：

```
evaluation/
├── tasks/           # 5 个编码任务定义
│   └── tasks.json   # t1~t5 含 prompt + pytest 测试代码
├── runners/         # 评测运行器
│   ├── run_eval.py  # 多 baseline 对比评测框架
│   ├── scorer.py    # 4 维度评分（功能60%+整洁15%+完整15%+安全10%）
│   └── code_postprocessor.py  # 确定性后处理器（后来加的）
├── results/         # 评测结果归档
└── workspace/       # 临时工作目录
```

### 3. 设计 5 个编码任务

| 编号 | 名称 | 难度 | 对应测试数 |
|------|------|------|-----------|
| t1 | FizzBuzz | easy | 5 |
| t2 | Word Frequency Counter | easy | 6 |
| t3 | Nested JSON Flattener | medium | 6 |
| t4 | CLI TODO Processor (文件IO) | medium | 5 |
| t5 | Stateful Calculator (类+异常) | medium-hard | 8 |

### 4. 第一轮评测（仅裸模型）

跑了一遍 A(bare_ollama) 验证管线通。

### 5. 关键发现

- ds-coder:1.3b 输出有大量碎碎念注释（`# type hint, as per requirement`）
- 输出不稳定：同一个 prompt 两次结果不同
- 缩进错误多发（`else :` 多一个空格 → IndentationError）

## 待办

- [x] 搭建完整评测框架
- [x] 5 个任务设计
- [ ] 后处理器
- [ ] 模型升级到 qwen2.5-coder:3b

## 技术决策

- 测评使用 HTTP API 直调 Ollama，不走 MiniAgent TypeScript 代码，避免框架耦合
- 评分用 pytest 单元测试而非人工判断
- 全部结果用 JSON 归档，支持重复实验
